$(document).ready(function(){
  	$('.own-blog-service').owlCarousel({
	    center: true,
	    items:2,
	    loop:true,
	    margin:30,
	})
	$('.own-img-service').owlCarousel({
		autoplay:true,
	    center: true,
	    items:9,
	    loop:true,
	    margin:0,
	})
});